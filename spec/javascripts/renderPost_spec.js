describe('our awesome test suite', function () {

  it('should append idea onto the page?', function () {
    var object = {name: 'test', body: 'test body', quality: 0}
    var results =
    assert.equal(renderPost(object),)
  });

});
